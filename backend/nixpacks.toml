[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "npm install",
  "pip install requests"
]

[phases.build]
cmds = [
  "python scripts/get_kalshi.py",
  "python scripts/get_polymarket.py",
  "python scripts/generate_embeddings.py"
]

[start]
cmd = "node server.js"
